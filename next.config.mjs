/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.iconscout.com',
      'i.pinimg.com',
      'www.shutterstock.com',
      'img.freepik.com',
    ],
  },
};

export default nextConfig;
